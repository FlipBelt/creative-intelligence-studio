# CIS 全局设计规范 V0.1

状态：开发前规划，待人工评审；非已实现界面。日期：2026-09-21。

## 1. 设计依据与冲突处理

用户指定 FlipBelt 知识平台的设计原则。实读本地对应仓库 README、V2 视觉规范、phase4-ui-optimization-spec.md 和 apps/web/src/styles.css。
参考本地 HEAD：467b8b0ba517e5e9928e70b30a42ecc746b76a26；origin 与用户 URL 一致。未核对远端最新 HEAD；本地存在无关未跟踪 .codex-worktrees/，未修改参考仓库。
历史文档描述米白，但当前 CSS 第 1–23 行定义纯白。CIS 优先采用当前 CSS 色值，将此差异显式记录，不声称生产页面已验证。
已有原则：编辑式层级、统一网格、细分隔线、内容优先、主色克制、避免阴影和卡片堆叠。
CIS 新提案：生产工作区允许宽布局、媒体网格和比较面板。新项目使用 CIS 文字标识，暂不移植 FlipBelt Logo。

## 2. 语义颜色

| 令牌 | 值 | 用途与限制 |
|---|---|---|
| ink | #151a17 | 主文字、主结构 |
| paper / paper-bright | #ffffff | 主页面、编辑区 |
| paper-deep | #f4f4f1 | 次级区、轻量 hover |
| acid | #e7ff3f | 主 CTA、选中；配 ink 文字 |
| acid-strong | #d9f900 | 强选中背景 |
| muted | #636963 | 辅助文字；实现时核验背景组合 |
| line | rgba(21,26,23,.2) | 装饰分隔，不承担唯一控件边界 |
| line-strong | rgba(21,26,23,.78) | 必要边界、结构 |
| apricot / rose / aqua | #f2aa78 / #e99da8 / #77c9c3 | 少量类型背景，配文字标签 |
| blue | #2478df | 来源/媒体信息装饰，正文链接仍用 ink + 下划线 |
| red | #d44832 | 错误标记；小字错误文案使用下述 error-text |
| error-text（新增） | #a12a1c | 待对比度验证的错误正文 |
| success-text（新增） | #21613b | 待验证的成功正文，始终带文字 |

不将 acid 当作成功或已审核的唯一标识。禁止渐变、玻璃效果、全站暗色翻新。深色只用于媒体观察区或局部信息带；媒体观察区不是暗色主题。

## 3. 排版与尺寸（CIS 提案）

字体沿用 Inter、Helvetica Neue、PingFang SC、Microsoft YaHei、Arial、sans-serif；不联网拉字体，不假设 Inter 已安装。
中文正文 16px / 1.65；表单与表格 14px / 1.5；辅助信息 12px / 1.5；不得机械复制参考仓库 9–11px 的操作文字。
工作区标题 28–36px；首页主标题 40–56px；章节标题 20–24px。大字号仅用于首页，生产页避免占据预览空间。
字重 400/500/600/700；中文正文不使用负字距。版本、成本、耗时用等宽数字；未知值显示“未知”而非 0。
间距阶梯 4/8/12/16/24/32/48/64px；常规行高至少 56px，密集行可 44px；触控目标 44px。
边线 1px；按钮/输入圆角 4px、标签 2px、媒体 4px；一般分区不加圆角或阴影。
标准按钮高 40px，触控场景 44px；主操作每个任务区域通常一个，危险操作远离主操作。

## 4. 布局

全局 Header 64px；标准页面最大宽 1240px，左右留白桌面 24px、手机 16px。各区共享对齐线。
桌面工作区 >=1280px：项目阶段导航 208px + 弹性主区 minmax(0,1fr) + 可收起证据栏 320px；栏间 24px。全宽工作区是明确例外。
768–1279px：阶段导航折叠；证据栏进入抽屉；主区优先。小于768px：单列、阶段选择器、详情底部面板。
360px 必须可完成阅读、需求编辑与审核；复杂板和视频时间轴可提供局部滚动及列表替代，不以“仅桌面可用”隐藏已有数据。
比较界面桌面并排 2–3 项，可选 3–5 个候选；移动端逐项切换且固定比较维度。
媒体预览采用 contain 保留完整产品，发现网格可 cover，但必须能打开未裁切原图。不得把缩略图裁切当作产品结构变化。
z-index 提案：基础0、sticky20、popover40、drawer60、dialog80、toast100。焦点不得被遮挡。

## 5. 组件契约

| 组件 | 变体/结构 | 必要行为 |
|---|---|---|
| AppHeader / StageNav | 标识、项目、阶段、账号 | 当前项短线+字重；语义 current；导航保留项目上下文 |
| PageHeading | 标题、说明、主操作 | 工作区紧凑，禁止每页大型 Hero |
| IndexRow | 标题、摘要、元数据、进入 | 行内独立动作不可嵌套交互，键盘可达 |
| FilterBar | 搜索、筛选、已选条件、清空 | 条件写 URL，返回保留条件 |
| Primary/Secondary/Text/Danger Button | 明确文字 | pending 防重复；禁用原因可见 |
| Field | 标签、输入、帮助、错误 | 必填含文字；错误关联输入；提交失败保留草稿 |
| StatusBadge / FactBadge | 文字+可选图标 | 状态与证据等级不混用，不仅靠颜色 |
| StatePanel | 标题、解释、下一动作 | loading、empty、error、permission 共用层级 |
| SourceRail | 来源、版本、时间、原文定位 | 无来源明确标未知；过期可见 |
| MediaTile / Preview | 缩略图、类型、状态、来源 | 破图回退；加载失败可重试；选中与打开分离 |
| ComparePanel | 同维度内容、依据、选择 | 不用单一总分代替解释；缺失数据不能默认为0 |
| JobRow | 阶段、真实状态、更新时间、动作 | 断流不假装失败/成功；无依据不显示百分比 |
| ReviewPanel | 产物、AI建议、事实问题、人工决定 | AI通过不能自动 Final；理由和版本留存 |
| Drawer / Dialog | 标题、内容、取消/确认 | 焦点管理、Escape、关闭后焦点返回 |
| Toast / InlineFeedback | 操作结果 | 持久错误留在原区；toast不承载唯一结果 |

## 6. 交互与动效

hover 只增强可点击性，关键说明同样支持 focus/tap。箭头可水平移动 2–4px，不带动整行布局。
交互反馈建议 120–180ms，抽屉 180–220ms，优先 opacity/transform。prefers-reduced-motion 下关闭位移，保留即时状态。
导航不自动滚动丢失位置；后台任务持续运行时离开页面给出可恢复入口。
表单显示“未保存 / 保存中 / 已保存 / 保存失败”；失败不得显示已保存。后端未支持自动保存时使用明确保存按钮。

## 7. 可访问性与一致性验收

目标：WCAG 2.2 AA；此为验收目标，非合规声明。正文对比度>=4.5:1，大字及必要非文本边界>=3:1；实际组合测量后才确认。
焦点建议统一 3px ink，深色区使用浅色焦点；focus顺序与视觉一致。支持 skip link、landmark、语义标题、原生按钮。
所有图标提供可访问名称；装饰图标 aria-hidden。图片提供有意义说明；视频预留字幕与文本转录。
验证 360/768/1440/1920px、200%缩放、键盘、reduced-motion、forced-colors；页面不整体横向溢出。
所有新增页面必须沿用本文件令牌，并覆盖 ux-spec.md 的状态矩阵。实际视觉与可用性由用户验收。

## 2026-09-21：面向非专业用户的首页与导航调整（已实施）
本节覆盖前述首版布局提案：桌面采用236px固定侧边栏，1100px以下208px，760px及以下使用可关闭的侧边栏抽屉。桌面不再设重复顶部导航。首页只保留创作入口、最近项目和辅助说明，不设置宣传轮播。
图片／文案／视频三个入口始终使用文字+图标；沿用现有apricot、acid、aqua令牌区分，不仅依赖颜色。项目主导航为描述需求、选择方向、查看与修改；专业工具与历史版本默认折叠。手机表单单列，主要按钮44px以上，支持Escape关闭导航。
